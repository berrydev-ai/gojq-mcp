# Example jq queries for sample.json

# Get all user names
.users[] | .name

# Filter users over 30
.users[] | select(.age > 30)

# Get total count
.total

# Transform to array of emails
[.users[] | .email]
